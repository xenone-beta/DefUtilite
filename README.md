# DefUtilite

**DefUtilite** — консольная утилита с полезными функциями для компьютера: поиск файлов/папок по имени, быстрое удаление по маске, подсчёт размера папки и очистка пустых директорий.

## Возможности

- 🔎 Поиск файлов и папок по имени или маске (`find`)
- 🗑️ Быстрое удаление файлов/папок по имени (`delete-name`)
- 📦 Подсчёт размера папки (`folder-size`)
- 🧹 Очистка пустых папок (`cleanup-empty`)

## Требования

- Python 3.10+

## Быстрый старт

```bash
python defutilite.py --help
```

## Команды

### 1) Поиск по имени

```bash
python defutilite.py find "*.log" --root /path/to/project
```

### 2) Удаление по имени/маске

Сначала предпросмотр:

```bash
python defutilite.py delete-name "*.tmp" --root /path/to/project --dry-run
```

Реальное удаление:

```bash
python defutilite.py delete-name "*.tmp" --root /path/to/project
```

### 3) Размер папки

```bash
python defutilite.py folder-size /path/to/folder
```

### 4) Удаление пустых папок

Предпросмотр:

```bash
python defutilite.py cleanup-empty --root /path/to/folder --dry-run
```

Удаление:

```bash
python defutilite.py cleanup-empty --root /path/to/folder
```

## Примечания

- Поддерживаются wildcard-шаблоны: `*`, `?`.
- Для read-only файлов утилита пытается снять ограничение и повторить удаление.
- Перед массовым удалением рекомендуется запуск с `--dry-run`.
